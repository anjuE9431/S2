echo "Enter a 5-digit number:"
read number

# Reverse the number
reverse=0
temp=$number

while [ $temp -ne 0 ]
do
    remainder=$((temp % 10))     
    reverse=$((reverse * 10 + remainder)) 
    temp=$((temp / 10))           
done


echo "The reverse of $number is $reverse"
